version: '3.8'

services:
  # PostgreSQL for Heidrun
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: heidrun_test
      POSTGRES_PASSWORD: test_password
      POSTGRES_DB: heidrun_test
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U heidrun_test"]
      interval: 5s
      timeout: 5s
      retries: 5

  heidrun-test:
    build:
      context: .
      dockerfile: Dockerfile.test
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      RUST_LOG: debug
      DATABASE_URL: postgres://heidrun_test:test_password@postgres:5432/heidrun_test
    volumes:
      - ./target:/app/target
      - ./tests:/app/tests
      - ./src:/app/src
    command: cargo test --release
