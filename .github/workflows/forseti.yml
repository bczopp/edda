# Forseti: ML/DL/RL Service CI
# Runs tests in container and lint (clippy, fmt).
name: Forseti CI

on:
  push:
    branches: [master, main]
    paths: ['forseti/**']
  pull_request:
    branches: [master, main]
    paths: ['forseti/**']

jobs:
  test:
    name: Test (container)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run tests in container
        run: |
          cd forseti
          docker compose -f docker-compose.test.yml run --rm forseti-test

  lint:
    name: Lint (fmt, clippy)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.75
          components: rustfmt, clippy

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            forseti/target
            ~/.cargo/registry
            ~/.cargo/git
          key: forseti-${{ runner.os }}-cargo-${{ hashFiles('forseti/Cargo.toml') }}
          restore-keys: forseti-${{ runner.os }}-cargo-

      - name: cargo fmt --check
        run: cargo fmt --check --manifest-path forseti/Cargo.toml

      - name: cargo clippy
        run: cargo clippy --manifest-path forseti/Cargo.toml -- -D warnings
