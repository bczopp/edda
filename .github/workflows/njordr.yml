# Njörðr: Marketplace Service CI
# Runs tests in container and lint (clippy, fmt).
name: Njörðr CI

on:
  push:
    branches: [master, main]
    paths: ['njordr/**']
  pull_request:
    branches: [master, main]
    paths: ['njordr/**']

jobs:
  test:
    name: Test (container)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run tests in container
        run: |
          cd njordr
          docker compose -f docker-compose.test.yml run --rm njordr-test

  lint:
    name: Lint (fmt, clippy)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          components: rustfmt, clippy

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            njordr/target
            ~/.cargo/registry
            ~/.cargo/git
          key: njordr-${{ runner.os }}-cargo-${{ hashFiles('njordr/Cargo.toml') }}
          restore-keys: njordr-${{ runner.os }}-cargo-

      - name: cargo fmt --check
        run: cargo fmt --check --manifest-path njordr/Cargo.toml

      - name: cargo clippy
        run: cargo clippy --manifest-path njordr/Cargo.toml -- -D warnings
