# Nornen: Decision Service CI
# Runs tests in container and lint (clippy, fmt).
name: Nornen CI

on:
  push:
    branches: [master, main]
    paths: ['nornen/**']
  pull_request:
    branches: [master, main]
    paths: ['nornen/**']

jobs:
  test:
    name: Test (container)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run tests in container
        run: |
          cd nornen
          docker compose -f docker-compose.test.yml run --rm nornen-test

  lint:
    name: Lint (fmt, clippy)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          components: rustfmt, clippy

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            nornen/target
            ~/.cargo/registry
            ~/.cargo/git
          key: nornen-${{ runner.os }}-cargo-${{ hashFiles('nornen/Cargo.toml') }}
          restore-keys: nornen-${{ runner.os }}-cargo-

      - name: cargo fmt --check
        run: cargo fmt --check --manifest-path nornen/Cargo.toml

      - name: cargo clippy
        run: cargo clippy --manifest-path nornen/Cargo.toml -- -D warnings
