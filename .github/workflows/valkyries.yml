# Valkyries: Coding Agent Plugin CI
# Runs tests in container and lint (clippy, fmt).
name: Valkyries CI

on:
  push:
    branches: [master, main]
    paths: ['valkyries/**']
  pull_request:
    branches: [master, main]
    paths: ['valkyries/**']

jobs:
  test:
    name: Test (container)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run tests in container
        run: |
          cd valkyries
          docker compose -f docker-compose.test.yml run --rm valkyries-test

  lint:
    name: Lint (fmt, clippy)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.75
          components: rustfmt, clippy

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            valkyries/target
            ~/.cargo/registry
            ~/.cargo/git
          key: valkyries-${{ runner.os }}-cargo-${{ hashFiles('valkyries/Cargo.toml') }}
          restore-keys: valkyries-${{ runner.os }}-cargo-

      - name: cargo fmt --check
        run: cargo fmt --check --manifest-path valkyries/Cargo.toml

      - name: cargo clippy
        run: cargo clippy --manifest-path valkyries/Cargo.toml -- -D warnings
