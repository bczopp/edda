version: '3.8'

services:
  alfheim-test:
    build:
      context: .
      dockerfile: Dockerfile.test
    environment:
      NODE_ENV: test
    volumes:
      - ./src:/app/src
      - ./tests:/app/tests
    command: bun test
